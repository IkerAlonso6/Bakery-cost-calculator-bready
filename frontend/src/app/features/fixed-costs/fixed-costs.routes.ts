import { Routes } from '@angular/router';
import { FixedCostListComponent } from './fixed-cost-list/fixed-cost-list.component';

export const FIXED_COSTS_ROUTES: Routes = [{ path: '', component: FixedCostListComponent }];
