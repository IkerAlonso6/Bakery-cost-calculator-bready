import { Routes } from '@angular/router';
import { CostSettingsPageComponent } from './cost-settings-page/cost-settings-page.component';

export const COST_SETTINGS_ROUTES: Routes = [{ path: '', component: CostSettingsPageComponent }];
