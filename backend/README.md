# Backend - Bakery Cost Calculator

Backend service for the Bakery Cost Calculator application, built with Java 17 and Spring Boot 3.2.

## Setup
- Ensure you have Java 17+ installed.
- Ensure you have Maven installed.
- Configure PostgreSQL database with the name `bakery_cost_calculator`.
- Run `mvn clean install` to build the project.
